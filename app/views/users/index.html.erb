<%= link_to "action1", url_for([:users, :action1, :format => :json]) %>
<%= link_to "action2", url_for([:users, :action2, :format => :json]) %>
<%= link_to "action3", url_for([:users, :action3, :format => :json]) %>
